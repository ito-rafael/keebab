[Unit]
Description=Monitor lan-mouse logs to trigger appropriate commands
# ensures that a manual stop/restart on lanmouse affects this as well
PartOf=lanmouse.service
# ensures systemd tries to start them together, but won't abort if lanmouse crashes
Wants=lanmouse.service
# ensure it starts after lanmouse
After=lanmouse.service

[Service]
ExecStart=/usr/bin/bash %h/.config/scripts/lanmouse-monitor.sh
Restart=on-failure
RestartSec=3
StandardOutput=journal
StandardError=journal
# set environment variables
Environment=WAYLAND_DISPLAY=wayland-1
Environment=XDG_RUNTIME_DIR=/run/user/%U
Environment=XDG_CONFIG_HOME=%h/.config

[Install]
# tie this service to lanmouse
WantedBy=lanmouse.service
// Include the system 'evdev' file
! include %S/evdev

! option = symbols
  fnkeys:fnkeys = +fnkeys(fnkeys)
  modifiers:modifiers = +modifiers(modifiers)

! option = compat
  capslockled:decouple = complete+capslockled(decouple)
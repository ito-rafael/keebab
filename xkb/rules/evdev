// Include the system 'evdev' file
! include %S/evdev

! option = symbols
  fn-keys:fnkeys = +fnkeys(fnkeys)
  modifiers:modifiers = +modifiers(modifiers)

! option = compat
  capslock-led:decouple = +capslock(decouple)